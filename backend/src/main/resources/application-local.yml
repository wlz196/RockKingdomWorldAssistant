# 本地开发配置 — 覆盖 application.yml 中环境相关的值
spring:
  datasource:
    url: jdbc:sqlite:/Users/wanglianzuo/project/RockKingdomWorldAssistant/roco_encyclopedia.db

logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.orm.jdbc.bind: TRACE
